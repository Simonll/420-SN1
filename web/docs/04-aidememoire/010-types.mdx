---
hide_table_of_contents: true
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import AideMemoireListe from '@site/docs/_components/AideMemoireListe.mdx';
import AideMemoireSet from '@site/docs/_components/AideMemoireSet.mdx';
import AideMemoireTuple from '@site/docs/_components/AideMemoireTuple.mdx';
import AideMemoireDict from '@site/docs/_components/AideMemoireDict.mdx';
import { S, N, K, F, B, C } from '@site/docs/_components/PyCode.mdx';

# Types de données

En Python, chaque valeur a un **type** qui détermine ce qu'on peut faire avec elle : additionner deux nombres, concaténer des textes, itérer sur une liste.

---

<Tabs queryString="onglet">

<TabItem value="base" label="Types de base" default>

Chaque valeur que tu manipules (entier, décimal, texte ou booléen) appartient à l'un de ces types.

| Type      | Description            | Exemple            |
|-----------|------------------------|--------------------|
| **int**   | Nombre entier           | <C>x = <N>42</N></C> |
| **float** | Nombre décimal          | <C>y = <N>3.14</N></C> |
| **str**   | Chaîne de caractères    | <C>nom = <S>&#34;Alice&#34;</S></C> |
| **bool**  | Booléen (vrai ou faux)  | <C>actif = <K>True</K></C> |

</TabItem>

<TabItem value="list" label="Liste (list)">

<AideMemoireListe />

</TabItem>

<TabItem value="set" label="Ensemble (set)">

<AideMemoireSet />

</TabItem>

<TabItem value="tuple" label="Tuple (tuple)">

<AideMemoireTuple />

</TabItem>

<TabItem value="dict" label="Dictionnaire (dict)">

<AideMemoireDict />

</TabItem>

<TabItem value="comparaison" label="Comparatif des collections">

### Caractéristiques des collections

Choisir la bonne structure dépend de ce dont tu as besoin : ordre, modifiabilité, unicité des éléments ou association clé-valeur.

| Structure | Ordonnée | Modifiable | Doublons permis | Accès | Ajout | Suppression |
|------------|-----------|--------------|------------------|-----------------------------|-----------------|----------------|
| **list**   | ✅ | ✅ | ✅ | Par index <C>[ ]</C> | <C>.<F>append</F>()</C>, <C>.<F>insert</F>()</C> | <C>.<F>remove</F>()</C>, <C>.<F>pop</F>()</C> |
| **tuple**  | ✅ | ❌ | ✅ | Par index <C>[ ]</C> | ❌ (immuable) | ❌ (immuable) |
| **set**    | ❌ | ✅ | ❌ | Test présence <C><K>in</K></C> | <C>.<F>add</F>()</C> | <C>.<F>remove</F>()</C>, <C>.<F>discard</F>()</C> |
| **dict**   | ✅ | ✅ | ❌ (clés uniques) | Par clé <C>d[<S>"clé"</S>]</C> | <C>d[<S>"clé"</S>] = valeur</C> | <C><K>del</K> d[<S>"clé"</S>]</C> |

**Note :** les **dict** conservent l'ordre d'insertion des clés.

### Utiliser le typage pour imposer des contraintes

Par exemple, il est possible de préciser qu'une list doit contenir uniquement des entiers, ou qu'un dict doit avoir des clés de type str et des valeurs de type int.

```python
from typing import List, Set, Tuple, Dict, Any

liste_a : List[int] = [1, 2, 3]  # Liste d'entiers
ensemble_b : Set[str] = {"a", "b", "c"}  # Ensemble
tuple_c : Tuple[int, str] = (1, "a")  # Tuple avec un entier et une chaîne
dictionnaire_d : Dict[str, int] = {"a": 1, "b": 2}  # Dictionnaire avec clés str et valeurs int
```

Il est également possible d'utiliser `Any` pour indiquer que n'importe quel type est accepté.

```pythonpython
from typing import List, Any
liste_e : List[Any] = [1, "a", 3.14, True]  # Liste pouvant contenir n'importe quel type
```




</TabItem>

</Tabs>
