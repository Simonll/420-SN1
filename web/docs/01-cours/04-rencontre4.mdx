---
title: 4 - Expressions booléennes
description: Expressions booléennes, if, elif, else, Comparaison, Imbrication, DRY, Forme compacte, raise/try/catch
hide_table_of_contents: true
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';
import Link from '@docusaurus/Link';
import Column from '../../src/components/Column';
import Row from '../../src/components/Row';
import { S, N, K, F, B, C } from '@site/docs/_components/PyCode.mdx';
import RappelFonctionnementClasseInverse from '@site/docs/_components/RappelFonctionnementClasseInverse.mdx';
import DocumentationAutorisee from '@site/docs/_components/DocumentationAutorisee.mdx';
import DocumentationInterdite from '@site/docs/_components/DocumentationInterdite.mdx';
import IntroSolutionsExercices from '@site/docs/_components/IntroSolutionsExercices.mdx';
import AideMemoireListe from '@site/docs/_components/AideMemoireListe.mdx';
import Mod_ExpressionsBooleennes from '../_cours-modules/_expressions-booleennes.mdx';
import Mod_If from '../_cours-modules/_if.mdx';
import Mod_Elif from '../_cours-modules/_elif.mdx';
import Mod_Else from '../_cours-modules/_else.mdx';
import Mod_Comparaison from '../_cours-modules/_comparaison.mdx';
import Mod_Imbrication from '../_cours-modules/_imbrication.mdx';
import Mod_DryConditions from '../_cours-modules/_dry-conditions.mdx';
import Mod_FormeCompacte from '../_cours-modules/_forme-compacte.mdx';
import Mod_ExceptionsValidation from '../_cours-modules/_exceptions-validation.mdx';
import Mod_TraceConditions from '../_cours-modules/_trace-conditions.mdx';

# Expressions booléennes

<Tabs queryString="onglet">
    <TabItem value="expressions" label="Expressions booléennes" default>
        <Mod_ExpressionsBooleennes />
    </TabItem>
    <TabItem value="if" label="if">
        <Mod_If />
    </TabItem>
    <TabItem value="elif" label="elif">
        <Mod_Elif />
    </TabItem>
    <TabItem value="else" label="else">
        <Mod_Else />
    </TabItem>
    <TabItem value="comparaison" label="Comparaison">
        <Mod_Comparaison />
    </TabItem>
    <TabItem value="imbrication" label="Imbrication">
        <Mod_Imbrication />
    </TabItem>
    <TabItem value="DRY" label="DRY">
        <Mod_DryConditions />
    </TabItem>
    <TabItem value="compacte" label="Forme compacte">
        <Mod_FormeCompacte />
    </TabItem>
    <TabItem value="exceptions-validation" label="Validation raise/try/except">
        <Mod_ExceptionsValidation />
    </TabItem>
    <TabItem value="trace" label="Trace">
        <Mod_TraceConditions />
    </TabItem>
    <TabItem value="Activités" label="Activités">


        <RappelFonctionnementClasseInverse/>
        <Tabs>
            <TabItem value="1" label=" Activités à faire à la maison">
                ## Auto-validation des connaissances

                [**Ce formulaire**](https://forms.gle/j4v5T4bW3Ay4q67R6) sert à vérifier votre compréhension des éléments les plus importants de la rencontre R04. Ne faites ce questionnaire **que lorsque vous vous sentez en très bonne maîtrise de la matière**. En classe, dès votre arrivée, vous aurez un **questionnaire très similaire**, évalué **sommativement**, à compléter sans accès à **aucune documentation**.

{/*                 ::::warning
                Ne pas remplir le formulaire diminue fortement vos chances de réussir l’évaluation sommative en début de rencontre. \
                Assurez-vous de bien comprendre toutes les notions derrière chaque question. */}
                ::::

                ---
                ## Exercices de compréhension

                Pour faire les traces des exercices suivants, regardez d'abord cette recette de [trace avec condition](/cours/rencontre4?onglet=trace)

                ::::info Exercice - Trace avec `if`/`else` + forme compacte

                :::note-nt ️
                🖨 Pour réaliser cet exercice, imprimez le [tableau de trace d'exécution](/pdf/Traces_recto_verso.pdf).
                :::

                :::warning-nt
                En situation d'**examen**, vous devrez compléter une trace d'exécution sur **papier** sans utiliser d'ordinateur.
                :::

                Sans exécuter le code, qu'affichera le programme ci-dessous? \
                En suivant **les recettes** de trace **[avec fonctions](/recettes/produire-une-trace?recette-activite=Avec+fonction)** et **[avec conditions](/recettes/produire-une-trace?recette-activite=condition)**, tracez son exécution.

                ```python showLineNumbers
                def carre(x):
                    return x * x

                def calcul(n):
                    if n % 2 == 0:
                        return n + 5
                    else:
                        return carre(n) - 1

                x = 3
                x = calcul(x)
                if x >= 5:
                    calcul(x)
                print("Hello World") if x == 13 else print("Fin")
                ```
                Validez **ensuite** votre trace en exécutant le programme en mode débogage.

                :::note Notions visées
                Structures conditionnelles (`if`/`else`), définition et appel de fonctions, `return`, opérateur modulo (`%`), forme compacte (ternaire), trace d'exécution, débogueur
                :::

                ::::

                ---

                ::::info Exercice - Trace avec `if`/`elif`/`elif`

                :::note-nt ️
                🖨 Pour réaliser cet exercice, imprimez le [tableau de trace d'exécution](/pdf/Traces_recto_verso.pdf).
                :::

                En suivant **les recettes** de trace **[avec fonctions](/recettes/produire-une-trace?recette-activite=Avec+fonction)** et **[avec conditions](/recettes/produire-une-trace?recette-activite=condition)**, tracez son exécution.

                ```python showLineNumbers
                def mystere(n):
                    if n % 2 == 0:
                        print("!")
                    elif n >= 10:
                        print("E")
                        if n // 10 == 5:
                            print("X")
                        print("P")
                    elif n >= 20:
                        print("F")

                mystere(55)
                print("33!")
                ```
                Validez **ensuite** votre trace en exécutant le programme en mode débogage.

                :::note Notions visées
                Structures conditionnelles (`if`/`elif`), conditions imbriquées, opérateurs (`%`, `//`, comparaisons), trace d'exécution, débogueur
                :::

                ::::

                ---

                ::::info Exercice - Trace avec opérateurs `and` et `or`

                :::note-nt ️
                🖨 Pour réaliser cet exercice, imprimez le [tableau de trace d'exécution](/pdf/Traces_recto_verso.pdf).
                :::

                En suivant **les recettes** de trace **[avec fonctions](/recettes/produire-une-trace?recette-activite=Avec+fonction)** et **[avec conditions](/recettes/produire-une-trace?recette-activite=condition)**, tracez son exécution.


                ```python showLineNumbers
                def mystere(x, y, z):
                    if (x >= 5 or y < 15) and z >= 10:
                        print("Cas 1")
                    if (x >= 5 and y < 15) or z >= 10:
                        print("Cas 2")


                mystere(0, 15, 15)
                mystere(5, 10, 15)
                ```

                Validez **ensuite** votre trace en exécutant le programme en mode débogage.

                :::note Notions visées
                Opérateurs logiques (`and`, `or`), opérateurs de comparaison, structures conditionnelles, priorité des opérateurs (parenthèses), trace d'exécution, débogueur
                :::

                ::::

                ---

                # Solutionnaires

                :::warning
                Consultez le solutionnaire **uniquement après avoir complété l'exercice par vous-même**. \
                Comparer votre réponse avec une solution vous permet de valider votre raisonnement et de corriger vos erreurs. \
                Consulter la solution avant d'avoir tenté l'exercice prive votre cerveau de l'occasion de construire des connexions durables.
                :::

                - Trace avec `if`/`else` + forme compacte — [solutionnaire.pdf](/pdf/r04_trace_if_else_solutionnaire.pdf)
                - Trace avec `if`/`elif`/`elif` — [solutionnaire.pdf](/pdf/r04_trace_if_elif_elif_solutionnaire.pdf)
                - Trace avec opérateurs `and` et `or` — [solutionnaire.pdf](/pdf/r04_trace_and_or_solutionnaire.pdf)

            </TabItem>


	        <TabItem value="2" label="Activités à faire en classe">

                ## Exercices de création

                Pour chacun des exercices de cette section, vous devez créer des fichiers `.py` avec les noms indiqués.

                <DocumentationAutorisee />

                ::::info Exercice - Le niveau de pH
                Créez un fichier `r04_niveau_ph.py` pour votre réponse.

                Définissez une fonction décrivant le niveau d'acidité d'une solution à partir de son [pH](https://fr.wikipedia.org/wiki/Potentiel_hydrog%C3%A8ne). \
                La fonction doit recevoir en entrée le pH et renvoyer en sortie soit : "acide", "neutre" ou "basique". \
                Validez le bon fonctionnement en testant les différents cas possibles à l'aide d'appels :
                ```python showLineNumbers
                print(f"Une solution avec un pH de 5 est : {nom_de_votre_fonction(5)}")
                print(f"Une solution avec un pH de 7 est : {nom_de_votre_fonction(7)}")
                print(f"Une solution avec un pH de 10 est : {nom_de_votre_fonction(10)}")
                ```

                <i>**Remarque** : si vous obtenez comme résultat "Une solution avec un pH de 5 est : None" c'est que votre fonction [ne retourne pas une valeur!](/cours/rencontre3?onglet=retour)</i>

                :::note Notions visées
                Définition de fonction, paramètre, structures conditionnelles (`if`/`elif`/`else`), `return`, f-strings
                :::

                ::::

                ---

                ::::info Exercice - Analyse d'échantillons chimiques 🧪
                Créez un fichier `r04_analyse_echantillons.py` pour votre réponse.

                Reprenez votre fonction `niveau_ph` de l'exercice précédent et utilisez-la pour analyser une série d'échantillons stockés dans des **listes parallèles**. \
                Vous **ne devez pas modifier** le code de la fonction **niveau_ph**, vous devez la réutiliser telle quelle.

                ```python
                echantillons = ["Eau distillée", "Jus de citron", "Eau de mer", "Lait", "Soude caustique"]
                ph_valeurs   = [7.0, 2.4, 8.1, 6.7, 13.5]
                ```

                Affiche l'analyse de chaque échantillon selon ce format :

                ✅ **Sortie attendue**
                ```
                Eau distillée   (pH 7.0)  → neutre
                Jus de citron   (pH 2.4)  → acide
                Eau de mer      (pH 8.1)  → basique
                Lait            (pH 6.7)  → acide
                Soude caustique (pH 13.5) → basique
                ```
                Pour obtenir les colonnes bien alignées, consultez la section **Alignement** de l'[aide-mémoire sur les f-strings](/aidememoire/f-string) \
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;en particulier la syntaxe `:<N` pour aligner un texte à gauche sur `N` caractères.


                :::note Notions visées
                Listes parallèles, accès par index, appel de fonction, f-strings
                :::

                ::::

                ---

                ::::info Exercice - REFACTORISATION : LES BULLETINS SCOLAIRES
                Créez un fichier `r04_refactorisation_bulletins.py` pour votre réponse.

                Le code ci-dessous affiche un mini-bulletin pour un.e étudiant.e. \
                Il fonctionne correctement, mais il est **truffé de répétitions inutiles** dans les blocs `if/elif/else`.

                Votre mission : le **réécrire proprement** en appliquant le principe **DRY**.

                ```python showLineNumbers
                nom = "Félix"
                note = 72

                if note >= 90:
                    print("=== Bulletin scolaire ===")
                    print(f"Étudiant : {nom}")
                    print(f"Note : {note} %")
                    print("Mention : Excellent")
                    print("Félicitations!")
                    print("=========================")
                elif note >= 75:
                    print("=== Bulletin scolaire ===")
                    print(f"Étudiant : {nom}")
                    print(f"Note : {note} %")
                    print("Mention : Bien")
                    print("Félicitations!")
                    print("=========================")
                elif note >= 60:
                    print("=== Bulletin scolaire ===")
                    print(f"Étudiant : {nom}")
                    print(f"Note : {note} %")
                    print("Mention : Passable")
                    print("Bonne continuation.")
                    print("=========================")
                else:
                    print("=== Bulletin scolaire ===")
                    print(f"Étudiant : {nom}")
                    print(f"Note : {note} %")
                    print("Mention : Échec")
                    print("Bonne continuation.")
                    print("=========================")
                ```

                #### **INSTRUCTIONS**

                1. **Identifiez** toutes les instructions redondantes dans le code ci-dessus : \
                   *Comptez combien de fois chaque `print` apparaît. Lesquels se retrouvent dans les **4 blocs**? Lesquels varient d'un bloc à l'autre?*

                2. **Créez une fonction** `afficher_bulletin(nom, note)` qui réécrit ce code proprement, sans répétition. \
                   Validez votre fonction avec ces deux appels :
                    ```python
                    afficher_bulletin("Félix", 72)
                    afficher_bulletin("Dayron", 91)
                    ```

                ✅ **Sortie attendue**
                ```
                === Bulletin scolaire ===
                Étudiant : Félix
                Note : 72 %
                Mention : Passable
                Bonne continuation.
                =========================
                === Bulletin scolaire ===
                Étudiant : Dayron
                Note : 91 %
                Mention : Excellent
                Félicitations!
                =========================
                ```

                :::note Notions visées
                Refactorisation (principe DRY), structures conditionnelles (`if`/`elif`/`else`), définition de fonction, f-strings
                :::

                ::::

                ---

                ::::info Exercice - Classification sismique 🌍
                Créez un fichier `r04_classification_sismique.py` pour votre réponse.

                Lors d'un tremblement de terre, deux mesures sont enregistrées : la **magnitude** (énergie libérée, échelle de Richter) et la **profondeur du foyer** en kilomètres. Ces deux valeurs, analysées ensemble, permettent d'estimer l'impact en surface.

                On veut écrire un programme qui affiche une **fiche sismique complète** à partir de ces deux mesures.

                #### Données de référence

                <div style={{ display: 'flex', justifyContent: 'center', gap: '2rem' }}>

                <div>

                **Classification par magnitude :**

                | Magnitude | Catégorie |
                |-----------|-----------|
                | < 2.0 | Micro |
                | < 4.0 | Mineur |
                | < 6.0 | Modéré |
                | < 7.0 | Fort |
                | ≥ 7.0 | Majeur |

                </div>

                <div>

                **Classification par profondeur du foyer :**

                | Profondeur (km) | Type de foyer |
                |-----------------|---------------|
                | ≤ 70 | Superficiel |
                | ≤ 300 | Intermédiaire |
                | > 300 | Profond |

                </div>

                </div>

                <p style={{ textAlign: 'center' }}><strong>Impact probable</strong> <em>(dépend des deux paramètres combinés)</em> :</p>

                <div style={{ display: 'flex', justifyContent: 'center' }}>

                | Magnitude | Profondeur | Impact probable |
                |-----------|------------|-----------------|
                | < 4.0 | peu importe | Généralement non ressenti ou ressenti légèrement. |
                | < 6.0 | ≤ 70 km | Ressenti clairement, légers dégâts possibles près de l'épicentre. |
                | < 6.0 | > 70 km | Ressenti faiblement en surface, peu de dégâts. |
                | < 7.0 | ≤ 70 km | Dommages importants près de l'épicentre, ressenti sur une large zone. |
                | < 7.0 | > 70 km | Ressenti sur une large zone, dommages modérés en surface. |
                | ≥ 7.0 | ≤ 70 km | Dommages majeurs, potentiellement catastrophique près de l'épicentre. |
                | ≥ 7.0 | > 70 km | Dommages importants malgré la profondeur, ressenti très largement. |

                </div>

                #### **INSTRUCTIONS**

                1. **Créez une fonction** `classifier_magnitude(magnitude)` qui retourne la catégorie correspondante (`"Micro"`, `"Mineur"`, `"Modéré"`, `"Fort"` ou `"Majeur"`).

                2. **Créez une fonction** `classifier_profondeur(profondeur_km)` qui retourne le type de foyer (`"Superficiel"`, `"Intermédiaire"` ou `"Profond"`).

                3. **Créez une fonction** `impact_probable(magnitude, profondeur_km)` qui retourne la description de l'impact en combinant les deux paramètres. \
                *Deux approches sont possibles pour cette fonction :*
                    - *Conditions **imbriquées** : on évalue d'abord la magnitude, puis on affine selon la profondeur à l'intérieur de chaque bloc (`if` dans un `if`).*
                    - *Conditions **combinées** avec `and` : on teste les deux critères sur une même ligne.*

                4. **Créez une fonction** `afficher_fiche(magnitude, profondeur_km)` qui appelle les trois fonctions précédentes et affiche la fiche selon le format ci-dessous.

                Validez votre programme avec les **4 cas** ci-dessous.

                ✅ **Cas 1** (`magnitude = 3.2`, `profondeur_km = 50`)
                ```
                === Fiche sismique ===
                Magnitude  : 3.2
                Profondeur : 50 km
                Catégorie  : Mineur
                Foyer      : Superficiel
                Impact     : Généralement non ressenti ou ressenti légèrement.
                ======================
                ```

                ✅ **Cas 2** (`magnitude = 5.1`, `profondeur_km = 30`)
                ```
                === Fiche sismique ===
                Magnitude  : 5.1
                Profondeur : 30 km
                Catégorie  : Modéré
                Foyer      : Superficiel
                Impact     : Ressenti clairement, légers dégâts possibles près de l'épicentre.
                ======================
                ```

                ✅ **Cas 3** (`magnitude = 6.2`, `profondeur_km = 150`)
                ```
                === Fiche sismique ===
                Magnitude  : 6.2
                Profondeur : 150 km
                Catégorie  : Fort
                Foyer      : Intermédiaire
                Impact     : Ressenti sur une large zone, dommages modérés en surface.
                ======================
                ```

                ✅ **Cas 4** (`magnitude = 7.8`, `profondeur_km = 20`)
                ```
                === Fiche sismique ===
                Magnitude  : 7.8
                Profondeur : 20 km
                Catégorie  : Majeur
                Foyer      : Superficiel
                Impact     : Dommages majeurs, potentiellement catastrophique près de l'épicentre.
                ======================
                ```

                :::note Notions visées
                Définition de fonctions, paramètres, `return`, structures conditionnelles (`if`/`elif`/`else`), conditions imbriquées, appel d'une fonction depuis une autre, f-strings
                :::

                ::::

                ---

                ::::info Exercice - TARIFICATION ÉLECTRIQUE PAR PALIERS ⚡
                Créez un fichier `r04_tarification_electrique.py` pour votre réponse.

                **Hydro-Québec** facture l'électricité résidentielle selon un système de **paliers** : \
                les premiers kilowattheures consommés coûtent moins cher, et la consommation supplémentaire est facturée à un tarif plus élevé.

                Voici une version simplifiée des tarifs :

                <div style={{ display: 'flex', justifyContent: 'center' }}>

                | Tarif | Palier | Prix |
                |-------|--------|------|
                | **Tarif D** (régulier) | Premiers 40 kWh/jour | 0,06 $/kWh |
                | **Tarif D** (régulier) | Au-delà de 40 kWh/jour | 0,10 $/kWh |
                | **Tarif nuit** (uniforme) | Toute la consommation | 0,05 $/kWh |

                </div>

                :::tip Exemple de calcul pour 65 kWh au Tarif D
                - Les **premiers 40 kWh** : 40 × 0,06 = **2,40 $**
                - Les **25 kWh restants** (65 − 40) : 25 × 0,10 = **2,50 $**
                - **Total : 4,90 $**

                La clé : quand `kwh > 40`, la facture se calcule en **deux parties**. On paie les 40 premiers kWh au tarif bas, puis le surplus au tarif élevé.
                :::

                #### **INSTRUCTIONS**

                1. **Créez une fonction** `calculer_facture(kwh, tarif_nuit=False)` qui calcule et **retourne** le montant à payer. \
                   *Le paramètre `tarif_nuit=False` est optionnel : par défaut, on utilise le tarif D. \
                   Si on passe `True`, on utilise le tarif nuit uniforme.* <br/><br/>
                   Validez avec ces appels avant de passer à la suite :
                    ```python
                    print(calculer_facture(30))           # Tarif D, 30 kWh  → 1.8
                    print(calculer_facture(65))           # Tarif D, 65 kWh  → 4.9
                    print(calculer_facture(65, True))     # Tarif nuit       → 3.25
                    ```

                2. **Créez une fonction** `afficher_comparaison(nom_client, kwh)` qui appelle `calculer_facture` deux fois : \
                   une fois sans argument optionnel (tarif D), une fois avec `True` (tarif nuit) et affiche ensuite la comparaison dans la console. <br/><br/>
                   Validez avec cet appel :
                    ```python
                    afficher_comparaison("Euclide", 65)
                    ```
                    ✅ Sortie attendue :
                    ```
                    Client : Euclide (65 kWh)
                        Tarif D    : 4.90 $
                        Tarif nuit : 3.25 $
                    ```
                <br/>
                :::note Notions visées
                Structures conditionnelles (paliers), définition de fonctions, paramètre avec valeur par défaut, `return`, appel d'une fonction depuis une autre
                :::

                ::::

                ---

                ::::info Exercice - TOURNOI DE QUIDDITCH
                Créez un fichier `r04_quidditch.py` pour votre réponse.

                La saison de Quidditch de Poudlard est terminée! \
                Les résultats des quatre maisons ont été compilés et sont déjà classés du premier au dernier. \
                Votre mission : afficher le classement officiel.

                ```python showLineNumbers
                maisons   = ["Gryffondor", "Serpentard", "Serdaigle", "Poufsouffle"]
                victoires = [3, 2, 1, 0]
                defaites  = [0, 1, 2, 3]
                points    = [450, 320, 280, 150]
                ```

                ✅ **Sortie attendue**
                ```
                ╔══════════════════════════════════════════╗
                ║     Tournoi de Quidditch de Poudlard     ║
                ╚══════════════════════════════════════════╝

                 1. Gryffondor   - 3V / 0D - 450 pts  ** Coupe des Quatre Maisons! **
                 2. Serpentard   - 2V / 1D - 320 pts
                 3. Serdaigle    - 1V / 2D - 280 pts
                 4. Poufsouffle  - 0V / 3D - 150 pts  [ Retourne a l’entrainement! ]

                ══════════════════════════════════════════
                ```

                **Contraintes :**
                - Définissez une fonction `afficher_maison` qui affiche la ligne de classement d'**une seule maison**.
                - Définissez une fonction `afficher_classement` qui affiche l'en-tête, appelle `afficher_maison` pour chaque maison, et affiche le séparateur final.
                - Le programme doit fonctionner peu importe les valeurs dans les listes. Elles auront toujours 4 éléments.
                - La première et la dernière maison du classement ont un message spécial.

                Pour obtenir les colonnes bien alignées, consultez la section **Alignement** de l'[aide-mémoire sur les f-strings](/aidememoire/f-string) \
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;en particulier la syntaxe `:<N` pour aligner un texte à gauche sur `N` caractères.

                :::note Notions visées
                Définition de fonctions, paramètres, `return`, structures conditionnelles (`if`/`elif`/`else`), listes parallèles, accès par index, f-strings, appel d’une fonction depuis une autre
                :::

                ::::

                ---

                ::::info Exercice - Validation de paramètres
                Créez un fichier `r04_validation_parametres.py` pour votre réponse.

                Écrivez une fonction `densite(masse, volume)` qui calcule et retourne la densité en g/mL selon la formule :

                $$
                d = \frac{m}{V}
                $$

                où $m$ est la masse en grammes (g) et $V$ le volume en millilitres (mL).

                - Validez que la masse est **positive**.
                - Validez que le volume est **strictement positif** (non nul).
                - Levez une exception claire (`ValueError` ou `ZeroDivisionError`) si les conditions ne sont pas respectées.

                Testez votre fonction en l'appelant avec des valeurs valides et invalides pour vérifier que la validation fonctionne correctement.

                Pourquoi est-il préférable d'**arrêter l'exécution avec un message clair** plutôt que de laisser Python planter avec une erreur brute?

                :::note Notions visées
                Définition de fonction, paramètres, `return`, validation de paramètres avec conditions, levée d'exceptions (`raise`, `ValueError`, `ZeroDivisionError`)
                :::

                ::::

                ---

                ## Exercices sur papier

                <DocumentationInterdite />

                ::::info Exercice - Indice de masse corporelle (IMC)
                Créez un fichier `r04_imc.py` pour votre réponse.

                Écrivez un programme qui calcule et interprète l'indice de masse corporelle (IMC) d'une personne selon la formule :

                $$
                IMC = \frac{masse}{taille^2}
                $$

                **Interprétation :**

                | IMC | Catégorie |
                |-----|-----------|
                | < 18.5 | Insuffisance pondérale |
                | < 25.0 | Poids normal |
                | < 30.0 | Surpoids |
                | ≥ 30.0 | Obésité |

                **Contraintes techniques :**
                - Définissez une fonction `calculer_imc(masse, taille)` qui retourne l'IMC arrondi à 1 décimale.
                - Définissez une fonction `interpreter_imc(imc)` qui retourne la catégorie correspondante (elle ne doit pas faire de `print`).
                - Affichez le résultat selon le format ci-dessous.

                Le début de votre programme est le suivant :
                ```python
                masse = 70    # en kg
                taille = 1.75 # en mètres
                ```

                **Exemple attendu :**
                ```
                Masse  : 70 kg
                Taille : 1.75 m
                IMC    : 22.9
                Catégorie : Poids normal
                ```

                :::note Notions visées
                Définition de fonctions, paramètres, `return`, structures conditionnelles (`if`/`elif`/`else`), opérateurs arithmétiques (`**`), format à 1 décimale
                :::

                ::::

                ---

                <IntroSolutionsExercices />

                Solution des **exercices de création** :
                - Le niveau de pH *(solutionnaire à venir)*
                - Analyse d'échantillons chimiques *(solutionnaire à venir)*
                - Refactorisation : les bulletins scolaires *(solutionnaire à venir)*
                - Classification sismique *(solutionnaire à venir)*
                - Tarification électrique par paliers *(solutionnaire à venir)*
                - Tournoi de Quidditch *(solutionnaire à venir)*
                - Validation de paramètres *(solutionnaire à venir)*

                Solution des **exercices sur papier** :
                - Indice de masse corporelle (IMC) *(solutionnaire à venir)*

            </TabItem>
        </Tabs>
    
    </TabItem>
    <TabItem value="match" label="*match/case*">


        :::warning match/case — pour ta culture générale!
        La structure `match/case` produit des résultats très similaires à un `if/elif/else` enchaîné.
        Elle n'est **pas exigée** dans ce cours : tu ne seras **jamais évalué** dessus.\
        Si tu la trouves intuitive, libre à toi de l'utiliser. Un bon vieux `if/elif/else` fera toujours l'affaire!
        :::

        :::info match/case
        Le `match/case` est une structure de contrôle qui permet de faire des correspondances de motifs (*pattern matching*).<br />
        - Il permet de comparer une variable à plusieurs valeurs possibles.<br />
        - Il offre ainsi une alternative aux longues chaînes d'instructions `if`/`elif`/`else` pour des cas spécifiques.

        Sa syntaxe est la suivante :
        ```python showLineNumbers
        match variable:
            case valeur1:
                # code à exécuter si variable == valeur1
            case valeur2:
                # code à exécuter si variable == valeur2
            case _:
                # code à exécuter si aucune des valeurs précédentes ne correspond
        ```
        - Le `_` dans le dernier `case` agit comme un `else`, capturant tous les autres cas non spécifiés.
        - Utile pour des comparaisons simples, mais peut être moins flexible que `if`/`elif`/`else` pour des conditions complexes.
        - Exemple :
        ```python showLineNumbers
        jour = "lundi"
        match jour:
            case "lundi":
                print("Début de la semaine")
            case "mercredi":
                print("Milieu de la semaine")
            case "vendredi":
                print("Fin de la semaine")
            case _:
                print("Jour ordinaire")
        ```
        :::

        :::tip Avantages du match/case
        - Plus lisible pour des comparaisons multiples sur une même variable.
        - Permet de gérer facilement des cas spécifiques.
        - Peut être plus performant que de nombreuses instructions `if`/`elif` dans certains cas.

        Il est important de noter que tout `match/case` peut être réécrit avec des `if`/`elif`/`else`, mais l'inverse n'est pas toujours vrai.
        :::


        {/* <Feedback feedbackId="Rencontre #4 - match/case" /> */}
    
    </TabItem>
</Tabs>
