---
title: 3 - Création de fonctions + TP1 poursuite
#description: Fonction, Paramètres, Retour, Portée, DRY, Commentaires, Pile d'appels
hide_table_of_contents: true
draft : true
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';
import Link from '@docusaurus/Link';
import Column from '../../src/components/Column';
import Row from '../../src/components/Row';
import { S, N, K, F, B, C } from '@site/docs/_components/PyCode.mdx';
import RappelFonctionnementClasseInverse from '@site/docs/_components/RappelFonctionnementClasseInverse.mdx';
import DocumentationAutorisee from '@site/docs/_components/DocumentationAutorisee.mdx';
import DocumentationInterdite from '@site/docs/_components/DocumentationInterdite.mdx';
import IntroSolutionsExercices from '@site/docs/_components/IntroSolutionsExercices.mdx';
import AideMemoireListe from '@site/docs/_components/AideMemoireListe.mdx';
import Mod_FonctionIntro from '../_cours-modules/_fonction-intro.mdx';
import Mod_ParamFonctions from '../_cours-modules/_param-fonctions.mdx';
import Mod_RetourFonctions from '../_cours-modules/_retour-fonctions.mdx';
import Mod_PorteeVariables from '../_cours-modules/_portee-variables.mdx';
import Mod_DryFonctions from '../_cours-modules/_dry-fonctions.mdx';
import Mod_Commentaires from '../_cours-modules/_commentaires.mdx';
import Mod_PileAppels from '../_cours-modules/_pile-appels.mdx';
import Mod_TraceSimple from '../_cours-modules/_trace-simple.mdx';

# Création de fonctions

<Tabs queryString="onglet">
    <TabItem value="intro" label="Fonction" default>
        <Mod_FonctionIntro />
    </TabItem>
    <TabItem value="parametres" label="Paramètres">
        <Mod_ParamFonctions />
    </TabItem>
    <TabItem value="retour" label="Retour">
        <Mod_RetourFonctions />
    </TabItem>
    <TabItem value="portee" label="Portée">
        <Mod_PorteeVariables />
    </TabItem>
    <TabItem value="DRY" label="DRY">
        <Mod_DryFonctions />
    </TabItem>
    <TabItem value="comments" label="Commentaires">
        <Mod_Commentaires />
    </TabItem>
    <TabItem value="pile" label="Pile d'appels">
        <Mod_PileAppels />
    </TabItem>
    <TabItem value="trace" label="Trace">
        <Mod_TraceSimple />
    </TabItem>
    <TabItem value="Activités" label="Activités">

        <RappelFonctionnementClasseInverse/>
        <Tabs>
            <TabItem value="1" label="🏠 Activités à faire à la maison">
                ## Auto-validation des connaissances

                [**Ce formulaire**](https://forms.gle/TJnBPSw6BYJDMa9k6) sert à vérifier votre compréhension des éléments les plus importants de la rencontre R03. Ne faites ce questionnaire **que lorsque vous vous sentez en très bonne maîtrise de la matière**. En classe, dès votre arrivée, vous aurez un **questionnaire très similaire**, évalué **sommativement**, à compléter sans accès à **aucune documentation**.

                ::::warning
                Ne pas remplir le formulaire diminue fortement vos chances de réussir l’évaluation sommative en début de rencontre. \
                Assurez-vous de bien comprendre toutes les notions derrière chaque question.
                ::::

                ---

                ## Exercices de compréhension

                Pour le premier exercice de cette section, vous pouvez écrire vos réponses sur papier ou vous pouvez [créer un fichier *markdown*](/recettes/creer-markdown). \
                Pour les autres exercices, vous pouvez imprimer ce [tableau de trace d'exécution](/pdf/Traces_recto_verso.pdf) pour réaliser les traces.


                ::::info Exercice - Portée des variables
                Créez un fichier `r03_portee_variables.md` pour votre réponse.

                :::warning-nt
                Il n’y a **aucune trace à rédiger** pour ce numéro.
                :::

                Soit le code suivant :

                ```python showLineNumbers
                def calcul_complexe(a, b, c):
                    x = a ** b
                    y = c * (b - a)
                    return x + y

                x = 5
                calcul_complexe(2, 3, 4)
                print(x)
                ```


                Combien vaut `x` dans calcul_complexe? (ligne 2)<br />
                Combien vaut `x` dans le script principal? (ligne 6)<br />
                S’agit‑il de la même variable `𝑥` ou de deux variables distinctes? Expliquez.

                :::note Notions visées
                Portée des variables (locale vs globale), paramètres de fonction, variables distinctes de même nom, `return`
                :::

                ::::

                ---

                ::::info Exercice - Trace avec fonction

                :::note-nt ️
                Pour réaliser cet exercice, imprimez le [tableau de trace d'exécution](/pdf/Traces_recto_verso.pdf).
                :::
                :::warning-nt
                En situation d'**examen**, vous devrez compléter une trace d'exécution sur **papier** sans utiliser d'ordinateur.
                :::

                Sans exécuter le code, déterminez ce que le programme ci-dessous va afficher dans la console. \\
                En suivant [**la recette de trace avec fonctions**](/recettes/produire-une-trace?recette-activite=Avec+fonction), complétez le tableau de trace d'exécution ligne par ligne.

                ```python showLineNumbers
                def fonction_lineaire(a, x, b) :
                    print("appel de la fonction")
                    z = a * x + b
                    return z

                x = 5
                y = 10
                z = 2
                r = fonction_lineaire(x, z, y) # Attention à l'ordre des paramètres
                x = x - 3
                print(r, x)
                ```
                Validez **ensuite** votre trace en exécutant le programme en mode débogage.

                :::note Notions visées
                Appel de fonction, paramètres et ordre des arguments, `return`, réaffectation de variables, trace d'exécution, débogueur
                :::

                ::::

                ---

                ::::info Exercice - Trace avec fonctions avec paramètres

                :::note-nt ️
                Pour réaliser cet exercice, imprimez le [tableau de trace d'exécution](/pdf/Traces_recto_verso.pdf).
                :::

                En suivant [**la recette de trace avec fonctions**](/recettes/produire-une-trace?recette-activite=Avec+fonction), complétez le tableau de trace d'exécution ligne par ligne.
                ```python showLineNumbers
                def ajoute_cinq(x):
                    return x + 5

                def carre(x):
                    return x * x

                def calcul_final(a, b):
                    print("appel de calcul_final")
                    return carre(a) + ajoute_cinq(b)

                x = 2
                resultat = calcul_final(x, 3)
                print(resultat)
                ```
                Validez **ensuite** votre trace en exécutant le programme en mode débogage.

                :::note Notions visées
                Appels de fonctions imbriqués, paramètres, `return`, ordre d'exécution, trace d'exécution, débogueur
                :::

                ::::

                ---

                # Solutionnaires

                :::warning
                Consultez le solutionnaire **uniquement après avoir complété l'exercice par vous-même**. \
                Comparer votre réponse avec une solution vous permet de valider votre raisonnement et de corriger vos erreurs. \
                Consulter la solution avant d'avoir tenté l'exercice prive votre cerveau de l'occasion de construire des connexions durables.
                :::

                - Portée des variables — [solutionnaire.pdf](/pdf/portee_variables_solutionnaire.pdf)
                - Trace avec fonction — [solutionnaire.pdf](/pdf/trace_avec_fonction_solutionnaire.pdf)
                - Trace avec fonctions avec paramètres — [solutionnaire.pdf](/pdf/trace_avec_fonctions_avec_parametres_solutionnaire.pdf)

            </TabItem>

	        <TabItem value="2" label=" 🏫 Activités à faire en classe">

                ## 🔨 Exercices de création

                Pour chacun des exercices de cette section, vous devez créer des fichiers `.py` avec les noms indiqués.

                <DocumentationAutorisee />

                ### 🟢 Exercices fondamentaux (à faire en priorité)

                Ces exercices couvrent les bases absolues : définition de fonction, paramètres, et retour de valeurs. **Faites-les d'abord.**

                ::::info Exercice - Conversion Celsius → Fahrenheit

                Créez un fichier `celsius_fahrenheit.py` pour votre réponse.

                Écrivez une fonction `celsius_vers_fahrenheit(celsius)` qui reçoit une température en **°C** et retourne sa valeur
                en **°F** grâce à la formule

                $$
                \text{°F} = \left( \text{°C} \times \frac{9}{5} \right) + 32
                $$

                Tester votre fonction à l'aide des instructions ci-dessous :
                ```python showLineNumbers
                print(celsius_vers_fahrenheit(0)) # 32.0
                print(celsius_vers_fahrenheit(100)) # 212.0
                ```

                :::note Notions visées
                Définition de fonction, paramètre, `return`, opérateurs arithmétiques
                :::

                ::::

                ---

                ::::info Exercice - Aire d’un rectangle

                Créez un fichier `aire_rectangle.py` pour votre réponse.

                Écrivez une fonction `aire_rectangle(longueur, largeur)` qui calcule et **retourne** l’aire d’un rectangle.<br />
                Puis, appelez la fonction pour afficher l’aire d’un rectangle de 5 m $\times$ 3 m. Par exemple :
                ```python showLineNumbers
                print(f"L'aire d'un rectangle de 5 x 3 est : {aire_rectangle(5,3)}") # L'aire d'un rectangle de 5 x 3 est : 15
                ```

                :::note Notions visées
                Définition de fonction, paramètres, `return`, appel de fonction, f-strings
                :::

                ::::

                ---

                ### 🟡 Exercices de composition et refactorisation

                Ces exercices approfondissent les notions : appeler une fonction depuis une autre, refactoriser du code répétitif, et utiliser les listes parallèles.

                ::::info Exercice - Réutilisation de fonctions
                Créez un fichier `reutilisation_fonctions.py` pour votre réponse.

                1) Écrivez une fonction `aire_rectangle(longueur, largeur)` qui calcule et **retourne** l’aire d’un rectangle.

                2) Écrivez une deuxième fonction `volume_prisme(longueur, largeur, hauteur)` qui calcule et **retourne** le
                volume d’un prisme rectangulaire.

                Cette deuxième fonction doit **réutiliser** (appeler) votre fonction `aire_rectangle` pour calculer la base du prisme avant de la
                multiplier par la hauteur.

                3) Testez vos fonctions en affichant :
                - l’aire d’un rectangle de $5 \times 3$
                - le volume d’un prisme de $5 \times 3 \times 4$

                *Objectif :* pratiquer la décomposition d’un problème en plusieurs fonctions réutilisables.

                :::note Notions visées
                Définition de fonctions, `return`, appel d'une fonction depuis une autre (réutilisation), décomposition d'un problème
                :::

                ::::

                ---

                ### 🟡 Exercices avec modules

                Ces exercices introduisent les **modules** : créer un fichier `.py` contenant des fonctions et l'importer dans un script.

                ::::info Exercice - ÉNERGIE CINÉTIQUE

                Créez un fichier `energie_cinetique.py` pour votre réponse. Le nom du fichier de module est au choix.

                Pour cet exercice, vous devez créer un **module** (un fichier `.py`) et un **script** (un autre fichier `.py`).<br />
                Référez-vous à la [recette sur les modules](/recettes/creer-module) pour savoir comment faire.

                Le module doit contenir une fonction, et le script doit importer ce module pour utiliser la fonction.

                1. Dans le module, nommé `sciences_nature`, écrivez une fonction permettant de calculer l'énergie cinétique $E$ en utilisant la formule :

                $$
                E = \frac{m \times v^2}{2},
                $$

                où $m$ est la masse et $v$ la vitesse.

                2.  Dans le script (un autre fichier `.py`) :
                    - importez le module,
                    - appelez votre fonction avec différentes valeurs,
                    - affichez les résultats.

                :::note Notions visées
                Modules (créer et importer), définition de fonction, `return`, opérateurs arithmétiques (`**`)
                :::

                ::::

                ---

                ::::info Exercice - FORCE GRAVITATIONNELLE
                Créez un fichier `force_gravitationnelle.py` pour votre réponse.

                Dans le même module créé à l'exercice précédent, ajoutez une fonction permettant de calculer le poids en utilisant la loi de la gravitation universelle de Newton dont la formule est :

                $$
                F = \frac{G \times m1 \times m2}{r^2},
                $$

                où :
                - $G = 6.674 \times 10^{-11}$ (constante gravitationnelle),
                - $m1$ et $m2$ sont les masses en kg,
                - $r$ est la distance en mètres.

                Dans un script Python (ça peut être le même qu'à l'exercice précédent) :
                - importez le module,
                - appelez votre fonction avec différentes valeurs,
                - affichez les résultats.

                :::note Notions visées
                Modules (créer et importer), définition de fonction, `return`, opérateurs arithmétiques (`**`)
                :::

                ::::

                ---

                ### 🟠 Exercices avancés : refactorisation et listes parallèles

                Ces exercices vous apprennent à **reconnaître le code répétitif** et à le **refactoriser** en utilisant des fonctions et des listes parallèles.

                ::::info Exercice - Bonjour

                Créez un fichier `bonjour.py` pour votre réponse.

                1) Écrivez une fonction `dire_bonjour(prenom)` qui reçoit un prénom en paramètre et affiche un message de salutation personnalisé. \
                Par exemple, appeler `dire_bonjour("Chatmouraï")` doit afficher :
                ```
                Bonjour, Chatmouraï!
                ```

                2) **Déclarez une liste** de prénoms, puis appelez `dire_bonjour` pour chaque élément en accédant aux éléments par leur index :
                ```python
                prenoms = ["Chatmouraï", "Astro", "Peluche"]
                ```

                Sortie attendue
                ```
                Bonjour, Chatmouraï!
                Bonjour, Astro!
                Bonjour, Peluche!
                ```

                :::note Notions visées
                Définition de fonction, paramètre, f-strings, listes, accès par index
                :::

                ::::

                ---

                ::::info Exercice - Refactorisation du code - Umbrella Corporation
                Créez un fichier `umbrella_corporation.py` pour votre réponse.

                Le code ci-dessous affiche un rapport concernant des expérimentations réalisées par une entreprise pharmaceutique de confiance. \
                **Beaucoup d'instructions** servant à afficher des données **sont très similaires** et sont utilisées dans le **même ordre**. \
                En programmation, c'est un **signe** qu'on doit **créer une fonction**!

                De plus, les données des échantillons peuvent être organisées dans des **listes parallèles** pour rendre le code encore plus lisible. \
                Améliorez ce code! Déclarez les listes parallèles, définissez une fonction et appelez-la.

                ```python showLineNumbers
                print("Rapport - Résultats des expériences Umbrella Corporation")

                print("Échantillon : Virus-T")
                print("Température : 37.0°C")
                print("pH : 6.2")
                print("Mutation Cellulaire : Stable")
                print("Contagiosité : Élevée")
                print("---------------")

                print("Échantillon : Virus-G")
                print("Température : 39.5°C")
                print("pH : 5.8")
                print("Mutation Cellulaire : Instable")
                print("Capacité Régénérative : Extrême")
                print("---------------")

                print("Échantillon : Uroboros")
                print("Température : 40.0°C")
                print("pH : 7.0")
                print("Mutation Cellulaire : Agressive")
                print("Compatibilité Hôte : Faible")
                print("---------------")

                print("Fin du rapport. Données classifiées - Niveau d'accès : Alpha")
                ```

                #### **INSTRUCTIONS**

                1. **Déclarez cinq listes parallèles** contenant les données des trois échantillons :
                    ```python
                    noms         = ["Virus-T", "Virus-G", "Uroboros"]
                    temperatures = [37.0, 39.5, 40.0]
                    pH_valeurs   = [6.2, 5.8, 7.0]
                    mutations    = ["Stable", "Instable", "Agressive"]
                    specifiques  = ["Contagiosité : Élevée", "Capacité Régénérative : Extrême", "Compatibilité Hôte : Faible"]
                    ```
                2. **Définissez une fonction** nommée `afficher_echantillon(...)` qui reçoit un élément de chacune des cinq listes en paramètre et affiche le bloc d'informations d'un échantillon.
                3. **Appelez la fonction** trois fois en lui passant les éléments correspondants par leur index (`noms[0]`, `temperatures[0]`, etc.).

                :::tip Pourquoi des listes parallèles?
                À l'index `0`, on retrouve tout ce qui concerne le Virus-T (`noms[0]`, `temperatures[0]`, etc.). Cette structure rend les données **plus faciles à maintenir** : ajouter un quatrième échantillon ne demande qu'un ajout dans chaque liste. Avec les boucles `for` que vous verrez bientôt, un seul appel de fonction suffirait pour traiter tous les échantillons!
                :::

                Lorsqu'on exécute votre version améliorée, on devrait observer le même résultat dans la console que la version originale.

                :::note Notions visées
                Refactorisation, détection de répétitions, définition de fonction, paramètres, listes parallèles, accès par index
                :::

                ::::

                ---

                ::::info Exercice - Refactorisation du code - Calcul de densité
                Créez un fichier `calcul_densite.py` pour votre réponse.

                Le code ci-dessous calcule la densité en kg/m<sup>3</sup> de 3 échantillons à partir de leur masse en g et de leur volume en cm<sup>3</sup>. \
                Le problème : les données sont stockées dans des variables individuelles et le calcul est répété trois fois. \
                Votre mission est de **réécrire ce programme** en mieux, en partant d'une page blanche.

                ```python showLineNumbers
                #Variables contenants les données de 3 échantillons
                echantillon1_masse_en_g = 135
                echantillon1_volume_cm3 = 84
                echantillon2_masse_en_g = 270
                echantillon2_volume_cm3 = 151
                echantillon3_masse_en_g = 92
                echantillon3_volume_cm3 = 113

                #Calcul des densités pour les 3 échantillons
                echantillon1_masse_en_kg = echantillon1_masse_en_g / 1000
                echantillon1_volume_m3 = echantillon1_volume_cm3 / 1_000_000
                echantillon1_densite = echantillon1_masse_en_kg / echantillon1_volume_m3
                echantillon2_masse_en_kg = echantillon2_masse_en_g / 1000
                echantillon2_volume_m3 = echantillon2_volume_cm3 / 1_000_000
                echantillon2_densite = echantillon2_masse_en_kg / echantillon2_volume_m3
                echantillon3_masse_en_kg = echantillon3_masse_en_g / 1000
                echantillon3_volume_m3 = echantillon3_volume_cm3 / 1_000_000
                echantillon3_densite = echantillon3_masse_en_kg / echantillon3_volume_m3

                #Affichage des résultats
                print("Densité 1 :", round(echantillon1_densite, 2), "kg/m3")
                print("Densité 2 :", round(echantillon2_densite, 2), "kg/m3")
                print("Densité 3 :", round(echantillon3_densite, 2), "kg/m3")
                ```

                #### 🛠️**INSTRUCTIONS**

                1. **Déclarez deux listes parallèles** `masses_g` et `volumes_cm3` contenant les données des 3 échantillons tirées du code ci-dessus.
                2. **Créez une fonction** `calculer_densite(masse_g, volume_cm3)` qui :
                    - convertit la masse de grammes en kilogrammes,
                    - convertit le volume de cm³ en m³,
                    - calcule et **retourne** la densité en kg/m³.
                3. **Appelez votre fonction** trois fois en accédant aux éléments des listes par leur index et stockez les résultats dans des variables.
                4. **Affichez les résultats** de façon identique à la version originale.

                :::tip Pourquoi des listes parallèles?
                Les masses et les volumes sont des données **du même type** collectées pour chaque échantillon. Les regrouper en listes parallèles réduit le nombre de variables de 6 à 2, et rend le code plus lisible : `masses_g[1]` et `volumes_cm3[1]` désignent clairement les données du deuxième échantillon. Avec les boucles `for` que vous verrez bientôt, un seul appel de fonction suffirait pour calculer les 3 densités!
                :::

                ✅ Sortie attendue
                ```
                Densité 1 : 1607.14 kg/m3
                Densité 2 : 1788.08 kg/m3
                Densité 3 : 814.16 kg/m3
                ```

                :::note Notions visées
                Refactorisation, listes parallèles, accès par index, définition de fonction, `return`, `round()`
                :::

                ::::

                ---

                ::::info Exercice - Une question de nanosecondes
                Créez un fichier `r03_nanosecondes.md` pour votre réponse.

                Dans la documentation du module `time` ([doc](https://docs.python.org/fr/3.13/library/time.html)), on trouve la fonction `time.monotonic_ns()`.
                Cette fonction permet  d'obtenir le temps en nanosecondes depuis le lancement de l'application. À chaque appel de la fonction, le temps retourné est toujours de plus en plus grand. Testez le code ci-dessous :

                ```python showLineNumbers
                import time

                print(f"Temps moment A: {time.monotonic_ns()}")
                print(f"Temps moment B: {time.monotonic_ns()}")
                print(f"Temps moment C: {time.monotonic_ns()}")
                ```

                Maintenant que vous comprenez ce que retourne cette fonction, observez son utilisation ci-dessous :

                ```python showLineNumbers
                import time

                def calcul_intensif():
                    '''
                    N'essayez VRAIMENT pas de comprendre le code de cette fonction!
                    Vous allez être en mesure de le comprendre à partir du cours sur les boucles.
                    La seule chose qui est importante de savoir, c'est que cette fonction prend un
                    certain temps pour s'exécuter (1 à 2 secondes en moyenne).
                    '''
                    total = 0
                    for i in range(1, 20000000):  # 20 millions d'itérations
                        total += i ** 0.5
                    return total

                tempA = time.monotonic_ns()
                calcul_intensif()
                tempB = time.monotonic_ns()

                mystere = tempB - tempA # Expression intéressante
                print(mystere)
                ```

                **Question** \
                Selon vous, en observant les instructions 15 à 19, que représente le nombre contenu dans la variable <i>mystere</i>?

                **Instructions pour vous aider dans votre réflexion**
                1) Exécutez le programme tel quel et notez la valeur affichée.
                2) À la ligne 11, modifiez le nombre 20 000 000 pour 40 000 000.
                3) Réexécutez le programme et notez le nouveau résultat.
                4) Expliquez la différence entre les 2 résultats obtenus.

                :::note Notions visées
                Module `time` (`monotonic_ns`), appel de fonction, mesure du temps d'exécution, lecture de documentation, f-strings
                :::

                ::::

                ---

                ## Exercices sur papier (pour l'examen)

                **Documentation interdite.** Ces exercices visent à préparer les évaluations sommatives où vous n'aurez pas accès à des ressources externes.

                ::::info Exercice - Force centripète
                Créez un fichier `force_centripete.py` pour votre réponse.

                Écrivez un programme qui calcule la force nécessaire ($F_c$) pour maintenir un objet en mouvement circulaire (comme une voiture dans un virage).

                **Formule :**
                $F_c = \frac{m \cdot v^2}{r}$<br/>(Note : $m$ est la masse en kg, $v$ la vitesse en m/s et $r$ le rayon du virage en mètres)

                **Contraintes techniques :**
                * Définissez une fonction nommée `calculer_force_centripete` recevant 3 paramètres : la masse, la vitesse et le rayon.
                * La fonction doit retourner la valeur calculée (elle ne doit pas faire de print elle-même).
                * Appelez la fonction, récupérez la valeur retournée dans une variable, puis affichez le résultat final avec deux décimales.



                **Exemple attendu (pour $m = 1200$, $v = 25$, $r = 50$) :**
                ```
                Paramètres : 1200 kg, 25 m/s, 50 m

                La force centripète résultante est de 15000.00 N
                ```

                :::note Notions visées
                Définition de fonction, paramètres, `return`, opérateurs arithmétiques (`**`), `round()` / format à 2 décimales
                :::

                ::::

                ---

                <IntroSolutionsExercices />

                🔨 Solution des **exercices de création** :
                - Conversion Celsius → Fahrenheit *(solutionnaire à venir)*
                - Aire d'un rectangle *(solutionnaire à venir)*
                - Réutilisation de fonctions *(solutionnaire à venir)*
                - Énergie cinétique *(solutionnaire à venir)*
                - Force gravitationnelle *(solutionnaire à venir)*
                - Bonjour *(solutionnaire à venir)*
                - Refactorisation du code - Umbrella Corporation *(solutionnaire à venir)*
                - Refactorisation du code - Calcul de densité *(solutionnaire à venir)*
                - Une question de nanosecondes *(solutionnaire à venir)*

                Solution des **exercices de compréhension** :
                - Force centripète *(solutionnaire à venir)*

            </TabItem>
        </Tabs>

    
    </TabItem>
    <TabItem value="param-fonction" label="*Fonction en paramètre*">


        :::warning
        Le passage de fonctions en paramètre est une notion plus avancée qui dépasse le cadre de ce cours.<br/>
        Elle n'est pas obligatoire pour réussir le cours, mais il s'agit d'un concept intéressant à explorer si vous êtes intéressé(e)s.<br />
        Vous pouvez choisir de ne pas lire cette section si vous n'êtes pas assez à l'aise ou si vous êtes déjà surmené(e)s!<br /><br />
        *Vous pouvez utiliser ou non cette notion pour réduire la quantité de code de la section **Comparaison des performances** du **TP1**.*
        :::

        ---

        :::info Les fonctions sont des objets

        En Python, une fonction est un objet comme un autre (comme un `int` ou une `str`). Cela signifie que vous pouvez :
        1. Stocker une fonction dans une variable.
        2. Passer une fonction en **paramètre** à une autre fonction.
        3. Retourner une fonction depuis une autre fonction.
        4. Sauvegarder une fonction dans une structure de données (liste, dictionnaire, etc.).
        5. Sauvegarder une fonction dans un fichier pour la réutiliser dans un autre fichier (module).
        :::

        ---

        :::tip-nt
        Imaginez un instrument de laboratoire qui lit une température en Celsius, mais qui doit pouvoir afficher le résultat dans différentes unités selon l'expérience en cours.
        :::

        ---

        ::::info Étape 1 : Créer des fonctions simples de transformation

        On commence par définir des fonctions simples qui font une seule tâche : la conversion.

        ```python showLineNumbers
        def vers_kelvin(celsius):
            return celsius + 273.15

        def vers_fahrenheit(celsius):
            return (celsius * 9/5) + 32
        ```
        ::::

        ---

        ::::info Étape 2 : Créer une fonction recevant une autre fonction en paramètre

        C'est ici que la magie opère. On crée une fonction qui accepte une valeur ET une **autre fonction** comme paramètre.

        ```python showLineNumbers
        # Le paramètre 'transformation' recevra une fonction (voir l'appel de cette fonction à l'étape #3)
        def afficher_resultat_labo(valeur_c, transformation):
            # On utilise le paramètre comme s'il s'agissait d'une fonction locale
            resultat = transformation(valeur_c)
            print(f"L'instrument affiche : {resultat}")
        ```
        ::::

        ---

        :::info Étape 3 : Appeler la fonction en lui passant une fonction en paramètre

        Lors de l'appel, on passe la fonction de conversion **sans les parenthèses**.\
        Si vous mettez des parenthèses, vous passez le *résultat* de la fonction au lieu de la fonction elle-même!

        ```python showLineNumbers
        temp_piece = 21.0

        # On passe 'vers_kelvin' comme un simple objet
        print("--- Mode Scientifique ---")
        afficher_resultat_labo(temp_piece, vers_kelvin)

        # On change le comportement simplement en passant une autre fonction
        print("--- Mode Américain ---")
        afficher_resultat_labo(temp_piece, vers_fahrenheit)
        ```
        :::

        ---

        :::note Pourquoi faire cela?

        Cela permet de créer une fonction "générique" (`afficher_resultat_labo()`) qui ne change jamais, même si vous ajoutez 50 nouveaux types de conversions plus tard. On sépare la **logique d'affichage** de la **logique de calcul**.
        :::

        ---

        :::info Code complet
        ```python showLineNumbers
        def vers_kelvin(celsius):
            return celsius + 273.15

        def vers_fahrenheit(celsius):
            return (celsius * 9/5) + 32

        def afficher_resultat_labo(valeur_c, transformation):
            resultat = transformation(valeur_c)
            print(f"L'instrument affiche : {resultat}")

        temp_piece = 21.0

        # On passe 'vers_kelvin' comme un simple objet
        print("--- Mode Scientifique ---")
        afficher_resultat_labo(temp_piece, vers_kelvin)

        # On change le comportement simplement en passant une autre fonction
        print("--- Mode Américain ---")
        afficher_resultat_labo(temp_piece, vers_fahrenheit)
        ```
        :::


        {/* <Feedback feedbackId="Rencontre #3 - Fonction en paramètre" /> */}
    
    </TabItem>
    <TabItem value="recusivite" label="*Récursivité*">


        :::warning
        La récursivité est une notion plus avancée qui dépasse le cadre de ce cours.<br/>
        Elle n'est pas obligatoire pour réussir le cours, mais il s'agit d'un concept intéressant à explorer si vous êtes intéressé(e)s.<br />
        Vous pouvez choisir de ne pas lire cette section si vous n'êtes pas assez à l'aise ou si vous êtes déjà surmené(e)s!<br />
        :::

        :::info Récursivité

        La récursivité est une technique de programmation où une fonction s'appelle elle-même pour résoudre un problème.<br />
        Cela peut être utile pour résoudre des problèmes qui peuvent être décomposés en sous-problèmes similaires.

        Voici un exemple simple de fonction récursive qui calcule la factorielle d'un nombre :<br />
        (note : le code contient un `if` que l'on n'a pas encore vu, mais c'est pour illustrer la récursivité)

        ```python showLineNumbers
        def factorielle(n):
            if n == 0 or n == 1:  # Cas de base
                return 1
            else:
                return n * factorielle(n - 1)  # Appel récursif

        print(factorielle(5))  # Affiche 120
        ```

        Dans cet exemple, la fonction `factorielle` s'appelle elle-même avec un paramètre réduit jusqu'à atteindre le cas de base (0 ou 1), où elle retourne 1.<br />
        La pile d'appels se remplit à chaque appel récursif, puis se vide lorsque les appels sont résolus.

        Si vous êtes intéressé(e)s, vous pouvez exécuter ce code en mode débogage et observer la pile d'appels pour voir comment elle évolue avec chaque appel récursif.

        :::


        {/* <Feedback feedbackId="Rencontre #3 - Récursivité" /> */}
    
    </TabItem>
</Tabs>
